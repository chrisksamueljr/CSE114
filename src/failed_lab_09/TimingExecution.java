public class TimingExecution {
    public static void main(String [] args) {s
        Scanner scan = new Scanner(System.in);
        System.out.println('enter you int and your upRange sepaerated by a space:  ');
        int size = scan.nextInt();
        int upRange = scan.nextInt();
         int [] ans = TimingExecution.random(size,upRange);
    
    }
    public static int[] random(int size, int upRange) {
        int[] num = new int [size];
        for(int index = 0; index < 100; int) {
            num[index] = int Math.random()*upRange;
        }
        return num;
    }
    public static void selectionSort(int []shelf) {
        for (int index =0; index < shelf.length; index++){
                int swap - shelf[index];
                int loc = 1;
                for(int hint =(index+1); index < shelf.length; shelf++){
                    if (min >)
                }
        } 
    }
}
